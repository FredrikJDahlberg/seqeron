# Whitelist: the build context is only what docker/Dockerfile copies. Without this the context is
# the whole tree, cmake-build-*/ included.
*
!build/install/seqeron
!docker/node-env.sh
!docker/entrypoint.sh
!docker/clusterctl
