/**
 * Support code, not what seqeron offers.
 *
 * <p>{@link org.limitless.seqeron.util.Clocks} (what every duty-cycle deadline here is measured against),
 * {@link org.limitless.seqeron.util.IdleStrategies} and {@link org.limitless.seqeron.util.Logger} ship in
 * the client tier because the classes above them use them. You may use them, but they are infrastructure
 * rather than API and carry no promise of stability.
 */
package org.limitless.seqeron.util;
